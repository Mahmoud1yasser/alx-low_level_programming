#include "main.h"

/**
 * mul - check the code.
 * Description : ' testing character to be digit'
 *@a: is first digit
 *@b: is second digit
 * Return: 0 or 1 (Success)
 */

int mul(int a, int b)
{
	int R;

	R = a * b;
	return (R);
}
