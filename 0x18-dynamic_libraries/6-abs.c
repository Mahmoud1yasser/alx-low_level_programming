#include "main.h"
#include<stdio.h>
/**
 * _abs - return absolute
 * Description: 'absolute'
 * @n: number
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
