#include "main.h"
#include<stdio.h>
/**
 * print_last_digit - prints last digit
 * Description: 'printer'
 * @n: number
 * Return: Always 0 (Success)
 */
int print_last_digit(int n)
{
	int r = n % 10;
	return (r);
}
